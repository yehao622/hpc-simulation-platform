server:
  port: 8080
  # servlet:
  #   context-path: /api

spring:
  application:
    name: smartops-data-processor
  # autoconfigure:
  #   exclude:
  #     - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
  #     - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  
  # profiles:
  #   active: ${SPRING_PROFILES_ACTIVE:development}
  
  # Database Configuration
  # datasource:
  #   url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:hpc_simulation}
  #   username: ${DB_USER:hpc_user}
  #   password: ${DB_PASSWORD:hpc_password}
  #   driver-class-name: org.postgresql.Driver
  #   hikari:
  #     maximum-pool-size: 10
  #     minimum-idle: 5
  #     idle-timeout: 300000
  #     max-lifetime: 1200000
  #     connection-timeout: 20000
  #     pool-name: SmartOpsDataProcessorPool
  
  # JPA/Hibernate Configuration
  # jpa:
  #   database: POSTGRESQL
  #   hibernate:
  #     ddl-auto: validate
  #     naming:
  #       physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.PostgreSQLDialect
  #       format_sql: false
  #       show_sql: false
  #       use_sql_comments: false
  #       jdbc:
  #         batch_size: 20
  #         batch_versioned_data: true
  #       order_inserts: true
  #       order_updates: true
  #       cache:
  #         use_second_level_cache: true
  #         use_query_cache: true
  #         region:
  #           factory_class: org.hibernate.cache.jcache.JCacheRegionFactory
  #   show-sql: false
  #   open-in-view: false
  
  # Redis Configuration
  # data:
  #   redis:
  #     host: ${REDIS_HOST:localhost}
  #     port: ${REDIS_PORT:6379}
  #     database: ${REDIS_DB:1}
  #     timeout: 2000ms
  #     jedis:
  #       pool:
  #         max-active: 8
  #         max-idle: 8
  #         min-idle: 0
  #         max-wait: -1ms
  
  # Cache Configuration
  # cache:
  #   type: redis
  #   cache-names:
  #     - jobMetrics
  #     - systemHealth
  #     - analyticsData
  #   redis:
  #     time-to-live: 300000  # 5 minutes
  #     cache-null-values: false
  
  # JSON Configuration
  # jackson:
  #   serialization:
  #     write-dates-as-timestamps: false
  #     indent-output: true
  #   deserialization:
  #     fail-on-unknown-properties: false
  #   time-zone: UTC

# Management and Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
        #,env,configprops, metrics,
      # base-path: /actuator
  endpoint:
    health:
      show-details: always
  #     show-components: always
  #   prometheus:
  #     enabled: true
  # metrics:
  #   export:
  #     prometheus:
  #       enabled: true
  #   tags:
  #     application: ${spring.application.name}
  #     environment: ${spring.profiles.active}
  # info:
  #   env:
  #     enabled: true
  #   java:
  #     enabled: true
  #   os:
  #     enabled: true

# Application-specific configuration
# smartops:
#   analytics:
#     batch-size: 100
#     cache-duration: 300  # seconds
#     max-results: 1000
#   metrics:
#     collection-interval: 30  # seconds
#     retention-days: 30
#   ai:
#     service-url: ${AI_SERVICE_URL:http://localhost:8000}
#     timeout: 5000  # ms

# Logging Configuration
# logging:
#   level:
#     root: INFO
#     com.smartops: DEBUG
#     #org.springframework.data.jpa: WARN
#     #org.hibernate.SQL: WARN
#     #org.hibernate.type.descriptor.sql.BasicBinder: WARN
#   pattern:
#     console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#     file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

---
# Development Profile
# spring:
#   config:
#     activate:
#       on-profile: development
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#   data:
#     redis:
#       database: 0

# logging:
#   level:
#     com.smartops: DEBUG
#     org.springframework.data.jpa: DEBUG

# smartops:
#   metrics:
#     collection-interval: 10

---
# Production Profile
# spring:
#   config:
#     activate:
#       on-profile: production
#   jpa:
#     hibernate:
#       ddl-auto: validate
#     show-sql: false

logging:
  level:
    root: INFO
    com.smartops: DEBUG

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,metrics,prometheus

# smartops:
#   analytics:
#     cache-duration: 600
#   metrics:
#     collection-interval: 60

---
# Test Profile  
# spring:
#   config:
#     activate:
#       on-profile: test
#   datasource:
#     url: jdbc:h2:mem:testdb
#     driver-class-name: org.h2.Driver
#     username: sa
#     password: password
#   jpa:
#     hibernate:
#       ddl-auto: create-drop
#     database: H2
#   data:
#     redis:
#       host: localhost
#       port: 6370